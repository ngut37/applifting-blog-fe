import { MessageObject } from '../../types';

export const headerMessages = Object.freeze<MessageObject>({
  recent_articles: 'Recent Articles',
  about: 'About',
  login: 'Log in',
  logout: 'Logout',
});

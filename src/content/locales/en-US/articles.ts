import { MessageObject } from '../../types';

export const articlesMessages = Object.freeze<MessageObject>({
  // LIST NO CONTENT
  'articles.no_content': 'No content 🤷‍♂️',

  // ARTICLE ITEM
  'articles.view_article': 'Read while article',
  'articles.comments.count': '{count} comments',
});
